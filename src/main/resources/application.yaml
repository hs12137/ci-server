spring:
  application:
    name: devopsTest
  datasource:
    url: jdbc:mysql://localhost:3306/devops_test?serverTimezone=Asia/Seoul
    username: root
    password: 1234
  jpa:
    hibernate:
      ddl-auto: none

