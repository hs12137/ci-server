spring:
  application:
    name: devopsTest
  datasource:
    url: jdbc:mysql://10.128.0.8:3307/devops_test?serverTimezone=Asia/Seoul
    username: ${username}
    password: ${password}
  jpa:
    hibernate:
      ddl-auto: none
